/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ExplainableAiTools } from "./ExplainableAiTools";
import { ExplainableAiToolsCountArgs } from "./ExplainableAiToolsCountArgs";
import { ExplainableAiToolsFindManyArgs } from "./ExplainableAiToolsFindManyArgs";
import { ExplainableAiToolsFindUniqueArgs } from "./ExplainableAiToolsFindUniqueArgs";
import { DeleteExplainableAiToolsArgs } from "./DeleteExplainableAiToolsArgs";
import { ExplainableAiToolsService } from "../explainableAiTools.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ExplainableAiTools)
export class ExplainableAiToolsResolverBase {
  constructor(
    protected readonly service: ExplainableAiToolsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ExplainableAiTools",
    action: "read",
    possession: "any",
  })
  async _explainableAiToolsItemsMeta(
    @graphql.Args() args: ExplainableAiToolsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ExplainableAiTools])
  @nestAccessControl.UseRoles({
    resource: "ExplainableAiTools",
    action: "read",
    possession: "any",
  })
  async explainableAiToolsItems(
    @graphql.Args() args: ExplainableAiToolsFindManyArgs
  ): Promise<ExplainableAiTools[]> {
    return this.service.explainableAiToolsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ExplainableAiTools, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ExplainableAiTools",
    action: "read",
    possession: "own",
  })
  async explainableAiTools(
    @graphql.Args() args: ExplainableAiToolsFindUniqueArgs
  ): Promise<ExplainableAiTools | null> {
    const result = await this.service.explainableAiTools(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ExplainableAiTools)
  @nestAccessControl.UseRoles({
    resource: "ExplainableAiTools",
    action: "delete",
    possession: "any",
  })
  async deleteExplainableAiTools(
    @graphql.Args() args: DeleteExplainableAiToolsArgs
  ): Promise<ExplainableAiTools | null> {
    try {
      return await this.service.deleteExplainableAiTools(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
