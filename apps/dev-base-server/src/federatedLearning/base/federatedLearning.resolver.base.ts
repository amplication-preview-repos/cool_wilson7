/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { FederatedLearning } from "./FederatedLearning";
import { FederatedLearningCountArgs } from "./FederatedLearningCountArgs";
import { FederatedLearningFindManyArgs } from "./FederatedLearningFindManyArgs";
import { FederatedLearningFindUniqueArgs } from "./FederatedLearningFindUniqueArgs";
import { DeleteFederatedLearningArgs } from "./DeleteFederatedLearningArgs";
import { FederatedLearningService } from "../federatedLearning.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => FederatedLearning)
export class FederatedLearningResolverBase {
  constructor(
    protected readonly service: FederatedLearningService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "FederatedLearning",
    action: "read",
    possession: "any",
  })
  async _federatedLearningsMeta(
    @graphql.Args() args: FederatedLearningCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [FederatedLearning])
  @nestAccessControl.UseRoles({
    resource: "FederatedLearning",
    action: "read",
    possession: "any",
  })
  async federatedLearnings(
    @graphql.Args() args: FederatedLearningFindManyArgs
  ): Promise<FederatedLearning[]> {
    return this.service.federatedLearnings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => FederatedLearning, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "FederatedLearning",
    action: "read",
    possession: "own",
  })
  async federatedLearning(
    @graphql.Args() args: FederatedLearningFindUniqueArgs
  ): Promise<FederatedLearning | null> {
    const result = await this.service.federatedLearning(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => FederatedLearning)
  @nestAccessControl.UseRoles({
    resource: "FederatedLearning",
    action: "delete",
    possession: "any",
  })
  async deleteFederatedLearning(
    @graphql.Args() args: DeleteFederatedLearningArgs
  ): Promise<FederatedLearning | null> {
    try {
      return await this.service.deleteFederatedLearning(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
