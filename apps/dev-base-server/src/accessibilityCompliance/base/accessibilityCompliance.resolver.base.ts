/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AccessibilityCompliance } from "./AccessibilityCompliance";
import { AccessibilityComplianceCountArgs } from "./AccessibilityComplianceCountArgs";
import { AccessibilityComplianceFindManyArgs } from "./AccessibilityComplianceFindManyArgs";
import { AccessibilityComplianceFindUniqueArgs } from "./AccessibilityComplianceFindUniqueArgs";
import { DeleteAccessibilityComplianceArgs } from "./DeleteAccessibilityComplianceArgs";
import { AccessibilityComplianceService } from "../accessibilityCompliance.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => AccessibilityCompliance)
export class AccessibilityComplianceResolverBase {
  constructor(
    protected readonly service: AccessibilityComplianceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "AccessibilityCompliance",
    action: "read",
    possession: "any",
  })
  async _accessibilityCompliancesMeta(
    @graphql.Args() args: AccessibilityComplianceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [AccessibilityCompliance])
  @nestAccessControl.UseRoles({
    resource: "AccessibilityCompliance",
    action: "read",
    possession: "any",
  })
  async accessibilityCompliances(
    @graphql.Args() args: AccessibilityComplianceFindManyArgs
  ): Promise<AccessibilityCompliance[]> {
    return this.service.accessibilityCompliances(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => AccessibilityCompliance, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "AccessibilityCompliance",
    action: "read",
    possession: "own",
  })
  async accessibilityCompliance(
    @graphql.Args() args: AccessibilityComplianceFindUniqueArgs
  ): Promise<AccessibilityCompliance | null> {
    const result = await this.service.accessibilityCompliance(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => AccessibilityCompliance)
  @nestAccessControl.UseRoles({
    resource: "AccessibilityCompliance",
    action: "delete",
    possession: "any",
  })
  async deleteAccessibilityCompliance(
    @graphql.Args() args: DeleteAccessibilityComplianceArgs
  ): Promise<AccessibilityCompliance | null> {
    try {
      return await this.service.deleteAccessibilityCompliance(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
