/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BlockchainAuditTrailsService } from "../blockchainAuditTrails.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BlockchainAuditTrailsCreateInput } from "./BlockchainAuditTrailsCreateInput";
import { BlockchainAuditTrails } from "./BlockchainAuditTrails";
import { BlockchainAuditTrailsFindManyArgs } from "./BlockchainAuditTrailsFindManyArgs";
import { BlockchainAuditTrailsWhereUniqueInput } from "./BlockchainAuditTrailsWhereUniqueInput";
import { BlockchainAuditTrailsUpdateInput } from "./BlockchainAuditTrailsUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BlockchainAuditTrailsControllerBase {
  constructor(
    protected readonly service: BlockchainAuditTrailsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BlockchainAuditTrails })
  @nestAccessControl.UseRoles({
    resource: "BlockchainAuditTrails",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBlockchainAuditTrails(
    @common.Body() data: BlockchainAuditTrailsCreateInput
  ): Promise<BlockchainAuditTrails> {
    return await this.service.createBlockchainAuditTrails({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BlockchainAuditTrails] })
  @ApiNestedQuery(BlockchainAuditTrailsFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BlockchainAuditTrails",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async blockchainAuditTrailsItems(
    @common.Req() request: Request
  ): Promise<BlockchainAuditTrails[]> {
    const args = plainToClass(BlockchainAuditTrailsFindManyArgs, request.query);
    return this.service.blockchainAuditTrailsItems({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BlockchainAuditTrails })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BlockchainAuditTrails",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async blockchainAuditTrails(
    @common.Param() params: BlockchainAuditTrailsWhereUniqueInput
  ): Promise<BlockchainAuditTrails | null> {
    const result = await this.service.blockchainAuditTrails({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BlockchainAuditTrails })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BlockchainAuditTrails",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBlockchainAuditTrails(
    @common.Param() params: BlockchainAuditTrailsWhereUniqueInput,
    @common.Body() data: BlockchainAuditTrailsUpdateInput
  ): Promise<BlockchainAuditTrails | null> {
    try {
      return await this.service.updateBlockchainAuditTrails({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BlockchainAuditTrails })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BlockchainAuditTrails",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBlockchainAuditTrails(
    @common.Param() params: BlockchainAuditTrailsWhereUniqueInput
  ): Promise<BlockchainAuditTrails | null> {
    try {
      return await this.service.deleteBlockchainAuditTrails({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
