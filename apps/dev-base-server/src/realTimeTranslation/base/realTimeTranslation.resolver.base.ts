/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { RealTimeTranslation } from "./RealTimeTranslation";
import { RealTimeTranslationCountArgs } from "./RealTimeTranslationCountArgs";
import { RealTimeTranslationFindManyArgs } from "./RealTimeTranslationFindManyArgs";
import { RealTimeTranslationFindUniqueArgs } from "./RealTimeTranslationFindUniqueArgs";
import { DeleteRealTimeTranslationArgs } from "./DeleteRealTimeTranslationArgs";
import { RealTimeTranslationService } from "../realTimeTranslation.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => RealTimeTranslation)
export class RealTimeTranslationResolverBase {
  constructor(
    protected readonly service: RealTimeTranslationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "RealTimeTranslation",
    action: "read",
    possession: "any",
  })
  async _realTimeTranslationsMeta(
    @graphql.Args() args: RealTimeTranslationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [RealTimeTranslation])
  @nestAccessControl.UseRoles({
    resource: "RealTimeTranslation",
    action: "read",
    possession: "any",
  })
  async realTimeTranslations(
    @graphql.Args() args: RealTimeTranslationFindManyArgs
  ): Promise<RealTimeTranslation[]> {
    return this.service.realTimeTranslations(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => RealTimeTranslation, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "RealTimeTranslation",
    action: "read",
    possession: "own",
  })
  async realTimeTranslation(
    @graphql.Args() args: RealTimeTranslationFindUniqueArgs
  ): Promise<RealTimeTranslation | null> {
    const result = await this.service.realTimeTranslation(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => RealTimeTranslation)
  @nestAccessControl.UseRoles({
    resource: "RealTimeTranslation",
    action: "delete",
    possession: "any",
  })
  async deleteRealTimeTranslation(
    @graphql.Args() args: DeleteRealTimeTranslationArgs
  ): Promise<RealTimeTranslation | null> {
    try {
      return await this.service.deleteRealTimeTranslation(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
