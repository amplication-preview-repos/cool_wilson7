/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CodeSynchronization } from "./CodeSynchronization";
import { CodeSynchronizationCountArgs } from "./CodeSynchronizationCountArgs";
import { CodeSynchronizationFindManyArgs } from "./CodeSynchronizationFindManyArgs";
import { CodeSynchronizationFindUniqueArgs } from "./CodeSynchronizationFindUniqueArgs";
import { DeleteCodeSynchronizationArgs } from "./DeleteCodeSynchronizationArgs";
import { CodeSynchronizationService } from "../codeSynchronization.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CodeSynchronization)
export class CodeSynchronizationResolverBase {
  constructor(
    protected readonly service: CodeSynchronizationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CodeSynchronization",
    action: "read",
    possession: "any",
  })
  async _codeSynchronizationsMeta(
    @graphql.Args() args: CodeSynchronizationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CodeSynchronization])
  @nestAccessControl.UseRoles({
    resource: "CodeSynchronization",
    action: "read",
    possession: "any",
  })
  async codeSynchronizations(
    @graphql.Args() args: CodeSynchronizationFindManyArgs
  ): Promise<CodeSynchronization[]> {
    return this.service.codeSynchronizations(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CodeSynchronization, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CodeSynchronization",
    action: "read",
    possession: "own",
  })
  async codeSynchronization(
    @graphql.Args() args: CodeSynchronizationFindUniqueArgs
  ): Promise<CodeSynchronization | null> {
    const result = await this.service.codeSynchronization(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CodeSynchronization)
  @nestAccessControl.UseRoles({
    resource: "CodeSynchronization",
    action: "delete",
    possession: "any",
  })
  async deleteCodeSynchronization(
    @graphql.Args() args: DeleteCodeSynchronizationArgs
  ): Promise<CodeSynchronization | null> {
    try {
      return await this.service.deleteCodeSynchronization(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
