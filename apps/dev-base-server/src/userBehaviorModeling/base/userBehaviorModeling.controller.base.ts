/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { UserBehaviorModelingService } from "../userBehaviorModeling.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { UserBehaviorModelingCreateInput } from "./UserBehaviorModelingCreateInput";
import { UserBehaviorModeling } from "./UserBehaviorModeling";
import { UserBehaviorModelingFindManyArgs } from "./UserBehaviorModelingFindManyArgs";
import { UserBehaviorModelingWhereUniqueInput } from "./UserBehaviorModelingWhereUniqueInput";
import { UserBehaviorModelingUpdateInput } from "./UserBehaviorModelingUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class UserBehaviorModelingControllerBase {
  constructor(
    protected readonly service: UserBehaviorModelingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: UserBehaviorModeling })
  @nestAccessControl.UseRoles({
    resource: "UserBehaviorModeling",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: UserBehaviorModelingCreateInput,
  })
  async createUserBehaviorModeling(
    @common.Body() data: UserBehaviorModelingCreateInput
  ): Promise<UserBehaviorModeling> {
    return await this.service.createUserBehaviorModeling({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [UserBehaviorModeling] })
  @ApiNestedQuery(UserBehaviorModelingFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "UserBehaviorModeling",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async userBehaviorModelings(
    @common.Req() request: Request
  ): Promise<UserBehaviorModeling[]> {
    const args = plainToClass(UserBehaviorModelingFindManyArgs, request.query);
    return this.service.userBehaviorModelings({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: UserBehaviorModeling })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserBehaviorModeling",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async userBehaviorModeling(
    @common.Param() params: UserBehaviorModelingWhereUniqueInput
  ): Promise<UserBehaviorModeling | null> {
    const result = await this.service.userBehaviorModeling({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: UserBehaviorModeling })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserBehaviorModeling",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: UserBehaviorModelingUpdateInput,
  })
  async updateUserBehaviorModeling(
    @common.Param() params: UserBehaviorModelingWhereUniqueInput,
    @common.Body() data: UserBehaviorModelingUpdateInput
  ): Promise<UserBehaviorModeling | null> {
    try {
      return await this.service.updateUserBehaviorModeling({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: UserBehaviorModeling })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserBehaviorModeling",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteUserBehaviorModeling(
    @common.Param() params: UserBehaviorModelingWhereUniqueInput
  ): Promise<UserBehaviorModeling | null> {
    try {
      return await this.service.deleteUserBehaviorModeling({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
