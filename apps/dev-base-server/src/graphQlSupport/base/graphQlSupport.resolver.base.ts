/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GraphQlSupport } from "./GraphQlSupport";
import { GraphQlSupportCountArgs } from "./GraphQlSupportCountArgs";
import { GraphQlSupportFindManyArgs } from "./GraphQlSupportFindManyArgs";
import { GraphQlSupportFindUniqueArgs } from "./GraphQlSupportFindUniqueArgs";
import { DeleteGraphQlSupportArgs } from "./DeleteGraphQlSupportArgs";
import { GraphQlSupportService } from "../graphQlSupport.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GraphQlSupport)
export class GraphQlSupportResolverBase {
  constructor(
    protected readonly service: GraphQlSupportService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GraphQlSupport",
    action: "read",
    possession: "any",
  })
  async _graphQlSupportsMeta(
    @graphql.Args() args: GraphQlSupportCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GraphQlSupport])
  @nestAccessControl.UseRoles({
    resource: "GraphQlSupport",
    action: "read",
    possession: "any",
  })
  async graphQlSupports(
    @graphql.Args() args: GraphQlSupportFindManyArgs
  ): Promise<GraphQlSupport[]> {
    return this.service.graphQlSupports(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GraphQlSupport, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GraphQlSupport",
    action: "read",
    possession: "own",
  })
  async graphQlSupport(
    @graphql.Args() args: GraphQlSupportFindUniqueArgs
  ): Promise<GraphQlSupport | null> {
    const result = await this.service.graphQlSupport(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GraphQlSupport)
  @nestAccessControl.UseRoles({
    resource: "GraphQlSupport",
    action: "delete",
    possession: "any",
  })
  async deleteGraphQlSupport(
    @graphql.Args() args: DeleteGraphQlSupportArgs
  ): Promise<GraphQlSupport | null> {
    try {
      return await this.service.deleteGraphQlSupport(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
