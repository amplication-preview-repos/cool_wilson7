/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { IntegratedKnowledgeBaseService } from "../integratedKnowledgeBase.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { IntegratedKnowledgeBaseCreateInput } from "./IntegratedKnowledgeBaseCreateInput";
import { IntegratedKnowledgeBase } from "./IntegratedKnowledgeBase";
import { IntegratedKnowledgeBaseFindManyArgs } from "./IntegratedKnowledgeBaseFindManyArgs";
import { IntegratedKnowledgeBaseWhereUniqueInput } from "./IntegratedKnowledgeBaseWhereUniqueInput";
import { IntegratedKnowledgeBaseUpdateInput } from "./IntegratedKnowledgeBaseUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class IntegratedKnowledgeBaseControllerBase {
  constructor(
    protected readonly service: IntegratedKnowledgeBaseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: IntegratedKnowledgeBase })
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createIntegratedKnowledgeBase(
    @common.Body() data: IntegratedKnowledgeBaseCreateInput
  ): Promise<IntegratedKnowledgeBase> {
    return await this.service.createIntegratedKnowledgeBase({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [IntegratedKnowledgeBase] })
  @ApiNestedQuery(IntegratedKnowledgeBaseFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async integratedKnowledgeBases(
    @common.Req() request: Request
  ): Promise<IntegratedKnowledgeBase[]> {
    const args = plainToClass(
      IntegratedKnowledgeBaseFindManyArgs,
      request.query
    );
    return this.service.integratedKnowledgeBases({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: IntegratedKnowledgeBase })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async integratedKnowledgeBase(
    @common.Param() params: IntegratedKnowledgeBaseWhereUniqueInput
  ): Promise<IntegratedKnowledgeBase | null> {
    const result = await this.service.integratedKnowledgeBase({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: IntegratedKnowledgeBase })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateIntegratedKnowledgeBase(
    @common.Param() params: IntegratedKnowledgeBaseWhereUniqueInput,
    @common.Body() data: IntegratedKnowledgeBaseUpdateInput
  ): Promise<IntegratedKnowledgeBase | null> {
    try {
      return await this.service.updateIntegratedKnowledgeBase({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: IntegratedKnowledgeBase })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteIntegratedKnowledgeBase(
    @common.Param() params: IntegratedKnowledgeBaseWhereUniqueInput
  ): Promise<IntegratedKnowledgeBase | null> {
    try {
      return await this.service.deleteIntegratedKnowledgeBase({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
