/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { IntegratedKnowledgeBase } from "./IntegratedKnowledgeBase";
import { IntegratedKnowledgeBaseCountArgs } from "./IntegratedKnowledgeBaseCountArgs";
import { IntegratedKnowledgeBaseFindManyArgs } from "./IntegratedKnowledgeBaseFindManyArgs";
import { IntegratedKnowledgeBaseFindUniqueArgs } from "./IntegratedKnowledgeBaseFindUniqueArgs";
import { DeleteIntegratedKnowledgeBaseArgs } from "./DeleteIntegratedKnowledgeBaseArgs";
import { IntegratedKnowledgeBaseService } from "../integratedKnowledgeBase.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => IntegratedKnowledgeBase)
export class IntegratedKnowledgeBaseResolverBase {
  constructor(
    protected readonly service: IntegratedKnowledgeBaseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "read",
    possession: "any",
  })
  async _integratedKnowledgeBasesMeta(
    @graphql.Args() args: IntegratedKnowledgeBaseCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [IntegratedKnowledgeBase])
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "read",
    possession: "any",
  })
  async integratedKnowledgeBases(
    @graphql.Args() args: IntegratedKnowledgeBaseFindManyArgs
  ): Promise<IntegratedKnowledgeBase[]> {
    return this.service.integratedKnowledgeBases(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => IntegratedKnowledgeBase, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "read",
    possession: "own",
  })
  async integratedKnowledgeBase(
    @graphql.Args() args: IntegratedKnowledgeBaseFindUniqueArgs
  ): Promise<IntegratedKnowledgeBase | null> {
    const result = await this.service.integratedKnowledgeBase(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => IntegratedKnowledgeBase)
  @nestAccessControl.UseRoles({
    resource: "IntegratedKnowledgeBase",
    action: "delete",
    possession: "any",
  })
  async deleteIntegratedKnowledgeBase(
    @graphql.Args() args: DeleteIntegratedKnowledgeBaseArgs
  ): Promise<IntegratedKnowledgeBase | null> {
    try {
      return await this.service.deleteIntegratedKnowledgeBase(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
