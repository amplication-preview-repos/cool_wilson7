/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { MultiModelDatabase } from "./MultiModelDatabase";
import { MultiModelDatabaseCountArgs } from "./MultiModelDatabaseCountArgs";
import { MultiModelDatabaseFindManyArgs } from "./MultiModelDatabaseFindManyArgs";
import { MultiModelDatabaseFindUniqueArgs } from "./MultiModelDatabaseFindUniqueArgs";
import { DeleteMultiModelDatabaseArgs } from "./DeleteMultiModelDatabaseArgs";
import { MultiModelDatabaseService } from "../multiModelDatabase.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => MultiModelDatabase)
export class MultiModelDatabaseResolverBase {
  constructor(
    protected readonly service: MultiModelDatabaseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "MultiModelDatabase",
    action: "read",
    possession: "any",
  })
  async _multiModelDatabasesMeta(
    @graphql.Args() args: MultiModelDatabaseCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [MultiModelDatabase])
  @nestAccessControl.UseRoles({
    resource: "MultiModelDatabase",
    action: "read",
    possession: "any",
  })
  async multiModelDatabases(
    @graphql.Args() args: MultiModelDatabaseFindManyArgs
  ): Promise<MultiModelDatabase[]> {
    return this.service.multiModelDatabases(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => MultiModelDatabase, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "MultiModelDatabase",
    action: "read",
    possession: "own",
  })
  async multiModelDatabase(
    @graphql.Args() args: MultiModelDatabaseFindUniqueArgs
  ): Promise<MultiModelDatabase | null> {
    const result = await this.service.multiModelDatabase(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => MultiModelDatabase)
  @nestAccessControl.UseRoles({
    resource: "MultiModelDatabase",
    action: "delete",
    possession: "any",
  })
  async deleteMultiModelDatabase(
    @graphql.Args() args: DeleteMultiModelDatabaseArgs
  ): Promise<MultiModelDatabase | null> {
    try {
      return await this.service.deleteMultiModelDatabase(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
