/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CanaryDeploymentsService } from "../canaryDeployments.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CanaryDeploymentsCreateInput } from "./CanaryDeploymentsCreateInput";
import { CanaryDeployments } from "./CanaryDeployments";
import { CanaryDeploymentsFindManyArgs } from "./CanaryDeploymentsFindManyArgs";
import { CanaryDeploymentsWhereUniqueInput } from "./CanaryDeploymentsWhereUniqueInput";
import { CanaryDeploymentsUpdateInput } from "./CanaryDeploymentsUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CanaryDeploymentsControllerBase {
  constructor(
    protected readonly service: CanaryDeploymentsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CanaryDeployments })
  @nestAccessControl.UseRoles({
    resource: "CanaryDeployments",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: CanaryDeploymentsCreateInput,
  })
  async createCanaryDeployments(
    @common.Body() data: CanaryDeploymentsCreateInput
  ): Promise<CanaryDeployments> {
    return await this.service.createCanaryDeployments({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CanaryDeployments] })
  @ApiNestedQuery(CanaryDeploymentsFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CanaryDeployments",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async canaryDeploymentsItems(
    @common.Req() request: Request
  ): Promise<CanaryDeployments[]> {
    const args = plainToClass(CanaryDeploymentsFindManyArgs, request.query);
    return this.service.canaryDeploymentsItems({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CanaryDeployments })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CanaryDeployments",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async canaryDeployments(
    @common.Param() params: CanaryDeploymentsWhereUniqueInput
  ): Promise<CanaryDeployments | null> {
    const result = await this.service.canaryDeployments({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CanaryDeployments })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CanaryDeployments",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: CanaryDeploymentsUpdateInput,
  })
  async updateCanaryDeployments(
    @common.Param() params: CanaryDeploymentsWhereUniqueInput,
    @common.Body() data: CanaryDeploymentsUpdateInput
  ): Promise<CanaryDeployments | null> {
    try {
      return await this.service.updateCanaryDeployments({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CanaryDeployments })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CanaryDeployments",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCanaryDeployments(
    @common.Param() params: CanaryDeploymentsWhereUniqueInput
  ): Promise<CanaryDeployments | null> {
    try {
      return await this.service.deleteCanaryDeployments({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
