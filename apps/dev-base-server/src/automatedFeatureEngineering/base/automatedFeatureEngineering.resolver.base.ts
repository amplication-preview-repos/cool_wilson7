/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AutomatedFeatureEngineering } from "./AutomatedFeatureEngineering";
import { AutomatedFeatureEngineeringCountArgs } from "./AutomatedFeatureEngineeringCountArgs";
import { AutomatedFeatureEngineeringFindManyArgs } from "./AutomatedFeatureEngineeringFindManyArgs";
import { AutomatedFeatureEngineeringFindUniqueArgs } from "./AutomatedFeatureEngineeringFindUniqueArgs";
import { DeleteAutomatedFeatureEngineeringArgs } from "./DeleteAutomatedFeatureEngineeringArgs";
import { AutomatedFeatureEngineeringService } from "../automatedFeatureEngineering.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => AutomatedFeatureEngineering)
export class AutomatedFeatureEngineeringResolverBase {
  constructor(
    protected readonly service: AutomatedFeatureEngineeringService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "AutomatedFeatureEngineering",
    action: "read",
    possession: "any",
  })
  async _automatedFeatureEngineeringsMeta(
    @graphql.Args() args: AutomatedFeatureEngineeringCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [AutomatedFeatureEngineering])
  @nestAccessControl.UseRoles({
    resource: "AutomatedFeatureEngineering",
    action: "read",
    possession: "any",
  })
  async automatedFeatureEngineerings(
    @graphql.Args() args: AutomatedFeatureEngineeringFindManyArgs
  ): Promise<AutomatedFeatureEngineering[]> {
    return this.service.automatedFeatureEngineerings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => AutomatedFeatureEngineering, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "AutomatedFeatureEngineering",
    action: "read",
    possession: "own",
  })
  async automatedFeatureEngineering(
    @graphql.Args() args: AutomatedFeatureEngineeringFindUniqueArgs
  ): Promise<AutomatedFeatureEngineering | null> {
    const result = await this.service.automatedFeatureEngineering(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => AutomatedFeatureEngineering)
  @nestAccessControl.UseRoles({
    resource: "AutomatedFeatureEngineering",
    action: "delete",
    possession: "any",
  })
  async deleteAutomatedFeatureEngineering(
    @graphql.Args() args: DeleteAutomatedFeatureEngineeringArgs
  ): Promise<AutomatedFeatureEngineering | null> {
    try {
      return await this.service.deleteAutomatedFeatureEngineering(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
