/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TimeSeriesDatabase } from "./TimeSeriesDatabase";
import { TimeSeriesDatabaseCountArgs } from "./TimeSeriesDatabaseCountArgs";
import { TimeSeriesDatabaseFindManyArgs } from "./TimeSeriesDatabaseFindManyArgs";
import { TimeSeriesDatabaseFindUniqueArgs } from "./TimeSeriesDatabaseFindUniqueArgs";
import { DeleteTimeSeriesDatabaseArgs } from "./DeleteTimeSeriesDatabaseArgs";
import { TimeSeriesDatabaseService } from "../timeSeriesDatabase.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => TimeSeriesDatabase)
export class TimeSeriesDatabaseResolverBase {
  constructor(
    protected readonly service: TimeSeriesDatabaseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "TimeSeriesDatabase",
    action: "read",
    possession: "any",
  })
  async _timeSeriesDatabasesMeta(
    @graphql.Args() args: TimeSeriesDatabaseCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [TimeSeriesDatabase])
  @nestAccessControl.UseRoles({
    resource: "TimeSeriesDatabase",
    action: "read",
    possession: "any",
  })
  async timeSeriesDatabases(
    @graphql.Args() args: TimeSeriesDatabaseFindManyArgs
  ): Promise<TimeSeriesDatabase[]> {
    return this.service.timeSeriesDatabases(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => TimeSeriesDatabase, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "TimeSeriesDatabase",
    action: "read",
    possession: "own",
  })
  async timeSeriesDatabase(
    @graphql.Args() args: TimeSeriesDatabaseFindUniqueArgs
  ): Promise<TimeSeriesDatabase | null> {
    const result = await this.service.timeSeriesDatabase(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => TimeSeriesDatabase)
  @nestAccessControl.UseRoles({
    resource: "TimeSeriesDatabase",
    action: "delete",
    possession: "any",
  })
  async deleteTimeSeriesDatabase(
    @graphql.Args() args: DeleteTimeSeriesDatabaseArgs
  ): Promise<TimeSeriesDatabase | null> {
    try {
      return await this.service.deleteTimeSeriesDatabase(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
