/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CarbonFootprintTrackingService } from "../carbonFootprintTracking.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CarbonFootprintTrackingCreateInput } from "./CarbonFootprintTrackingCreateInput";
import { CarbonFootprintTracking } from "./CarbonFootprintTracking";
import { CarbonFootprintTrackingFindManyArgs } from "./CarbonFootprintTrackingFindManyArgs";
import { CarbonFootprintTrackingWhereUniqueInput } from "./CarbonFootprintTrackingWhereUniqueInput";
import { CarbonFootprintTrackingUpdateInput } from "./CarbonFootprintTrackingUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CarbonFootprintTrackingControllerBase {
  constructor(
    protected readonly service: CarbonFootprintTrackingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CarbonFootprintTracking })
  @nestAccessControl.UseRoles({
    resource: "CarbonFootprintTracking",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCarbonFootprintTracking(
    @common.Body() data: CarbonFootprintTrackingCreateInput
  ): Promise<CarbonFootprintTracking> {
    return await this.service.createCarbonFootprintTracking({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CarbonFootprintTracking] })
  @ApiNestedQuery(CarbonFootprintTrackingFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CarbonFootprintTracking",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async carbonFootprintTrackings(
    @common.Req() request: Request
  ): Promise<CarbonFootprintTracking[]> {
    const args = plainToClass(
      CarbonFootprintTrackingFindManyArgs,
      request.query
    );
    return this.service.carbonFootprintTrackings({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CarbonFootprintTracking })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CarbonFootprintTracking",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async carbonFootprintTracking(
    @common.Param() params: CarbonFootprintTrackingWhereUniqueInput
  ): Promise<CarbonFootprintTracking | null> {
    const result = await this.service.carbonFootprintTracking({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CarbonFootprintTracking })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CarbonFootprintTracking",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCarbonFootprintTracking(
    @common.Param() params: CarbonFootprintTrackingWhereUniqueInput,
    @common.Body() data: CarbonFootprintTrackingUpdateInput
  ): Promise<CarbonFootprintTracking | null> {
    try {
      return await this.service.updateCarbonFootprintTracking({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CarbonFootprintTracking })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CarbonFootprintTracking",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCarbonFootprintTracking(
    @common.Param() params: CarbonFootprintTrackingWhereUniqueInput
  ): Promise<CarbonFootprintTracking | null> {
    try {
      return await this.service.deleteCarbonFootprintTracking({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
