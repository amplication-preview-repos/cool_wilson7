/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ZeroTrustSecurityService } from "../zeroTrustSecurity.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ZeroTrustSecurityCreateInput } from "./ZeroTrustSecurityCreateInput";
import { ZeroTrustSecurity } from "./ZeroTrustSecurity";
import { ZeroTrustSecurityFindManyArgs } from "./ZeroTrustSecurityFindManyArgs";
import { ZeroTrustSecurityWhereUniqueInput } from "./ZeroTrustSecurityWhereUniqueInput";
import { ZeroTrustSecurityUpdateInput } from "./ZeroTrustSecurityUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ZeroTrustSecurityControllerBase {
  constructor(
    protected readonly service: ZeroTrustSecurityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ZeroTrustSecurity })
  @nestAccessControl.UseRoles({
    resource: "ZeroTrustSecurity",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createZeroTrustSecurity(
    @common.Body() data: ZeroTrustSecurityCreateInput
  ): Promise<ZeroTrustSecurity> {
    return await this.service.createZeroTrustSecurity({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ZeroTrustSecurity] })
  @ApiNestedQuery(ZeroTrustSecurityFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ZeroTrustSecurity",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async zeroTrustSecurities(
    @common.Req() request: Request
  ): Promise<ZeroTrustSecurity[]> {
    const args = plainToClass(ZeroTrustSecurityFindManyArgs, request.query);
    return this.service.zeroTrustSecurities({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ZeroTrustSecurity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ZeroTrustSecurity",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async zeroTrustSecurity(
    @common.Param() params: ZeroTrustSecurityWhereUniqueInput
  ): Promise<ZeroTrustSecurity | null> {
    const result = await this.service.zeroTrustSecurity({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ZeroTrustSecurity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ZeroTrustSecurity",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateZeroTrustSecurity(
    @common.Param() params: ZeroTrustSecurityWhereUniqueInput,
    @common.Body() data: ZeroTrustSecurityUpdateInput
  ): Promise<ZeroTrustSecurity | null> {
    try {
      return await this.service.updateZeroTrustSecurity({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ZeroTrustSecurity })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ZeroTrustSecurity",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteZeroTrustSecurity(
    @common.Param() params: ZeroTrustSecurityWhereUniqueInput
  ): Promise<ZeroTrustSecurity | null> {
    try {
      return await this.service.deleteZeroTrustSecurity({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
