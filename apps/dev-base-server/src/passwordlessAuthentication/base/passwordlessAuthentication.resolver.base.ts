/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PasswordlessAuthentication } from "./PasswordlessAuthentication";
import { PasswordlessAuthenticationCountArgs } from "./PasswordlessAuthenticationCountArgs";
import { PasswordlessAuthenticationFindManyArgs } from "./PasswordlessAuthenticationFindManyArgs";
import { PasswordlessAuthenticationFindUniqueArgs } from "./PasswordlessAuthenticationFindUniqueArgs";
import { DeletePasswordlessAuthenticationArgs } from "./DeletePasswordlessAuthenticationArgs";
import { PasswordlessAuthenticationService } from "../passwordlessAuthentication.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PasswordlessAuthentication)
export class PasswordlessAuthenticationResolverBase {
  constructor(
    protected readonly service: PasswordlessAuthenticationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PasswordlessAuthentication",
    action: "read",
    possession: "any",
  })
  async _passwordlessAuthenticationsMeta(
    @graphql.Args() args: PasswordlessAuthenticationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PasswordlessAuthentication])
  @nestAccessControl.UseRoles({
    resource: "PasswordlessAuthentication",
    action: "read",
    possession: "any",
  })
  async passwordlessAuthentications(
    @graphql.Args() args: PasswordlessAuthenticationFindManyArgs
  ): Promise<PasswordlessAuthentication[]> {
    return this.service.passwordlessAuthentications(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PasswordlessAuthentication, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PasswordlessAuthentication",
    action: "read",
    possession: "own",
  })
  async passwordlessAuthentication(
    @graphql.Args() args: PasswordlessAuthenticationFindUniqueArgs
  ): Promise<PasswordlessAuthentication | null> {
    const result = await this.service.passwordlessAuthentication(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PasswordlessAuthentication)
  @nestAccessControl.UseRoles({
    resource: "PasswordlessAuthentication",
    action: "delete",
    possession: "any",
  })
  async deletePasswordlessAuthentication(
    @graphql.Args() args: DeletePasswordlessAuthenticationArgs
  ): Promise<PasswordlessAuthentication | null> {
    try {
      return await this.service.deletePasswordlessAuthentication(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
