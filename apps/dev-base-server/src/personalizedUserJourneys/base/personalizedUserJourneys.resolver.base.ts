/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PersonalizedUserJourneys } from "./PersonalizedUserJourneys";
import { PersonalizedUserJourneysCountArgs } from "./PersonalizedUserJourneysCountArgs";
import { PersonalizedUserJourneysFindManyArgs } from "./PersonalizedUserJourneysFindManyArgs";
import { PersonalizedUserJourneysFindUniqueArgs } from "./PersonalizedUserJourneysFindUniqueArgs";
import { DeletePersonalizedUserJourneysArgs } from "./DeletePersonalizedUserJourneysArgs";
import { PersonalizedUserJourneysService } from "../personalizedUserJourneys.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PersonalizedUserJourneys)
export class PersonalizedUserJourneysResolverBase {
  constructor(
    protected readonly service: PersonalizedUserJourneysService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PersonalizedUserJourneys",
    action: "read",
    possession: "any",
  })
  async _personalizedUserJourneysItemsMeta(
    @graphql.Args() args: PersonalizedUserJourneysCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PersonalizedUserJourneys])
  @nestAccessControl.UseRoles({
    resource: "PersonalizedUserJourneys",
    action: "read",
    possession: "any",
  })
  async personalizedUserJourneysItems(
    @graphql.Args() args: PersonalizedUserJourneysFindManyArgs
  ): Promise<PersonalizedUserJourneys[]> {
    return this.service.personalizedUserJourneysItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PersonalizedUserJourneys, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PersonalizedUserJourneys",
    action: "read",
    possession: "own",
  })
  async personalizedUserJourneys(
    @graphql.Args() args: PersonalizedUserJourneysFindUniqueArgs
  ): Promise<PersonalizedUserJourneys | null> {
    const result = await this.service.personalizedUserJourneys(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PersonalizedUserJourneys)
  @nestAccessControl.UseRoles({
    resource: "PersonalizedUserJourneys",
    action: "delete",
    possession: "any",
  })
  async deletePersonalizedUserJourneys(
    @graphql.Args() args: DeletePersonalizedUserJourneysArgs
  ): Promise<PersonalizedUserJourneys | null> {
    try {
      return await this.service.deletePersonalizedUserJourneys(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
