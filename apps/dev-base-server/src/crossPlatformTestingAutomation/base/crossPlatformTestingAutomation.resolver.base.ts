/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CrossPlatformTestingAutomation } from "./CrossPlatformTestingAutomation";
import { CrossPlatformTestingAutomationCountArgs } from "./CrossPlatformTestingAutomationCountArgs";
import { CrossPlatformTestingAutomationFindManyArgs } from "./CrossPlatformTestingAutomationFindManyArgs";
import { CrossPlatformTestingAutomationFindUniqueArgs } from "./CrossPlatformTestingAutomationFindUniqueArgs";
import { DeleteCrossPlatformTestingAutomationArgs } from "./DeleteCrossPlatformTestingAutomationArgs";
import { CrossPlatformTestingAutomationService } from "../crossPlatformTestingAutomation.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CrossPlatformTestingAutomation)
export class CrossPlatformTestingAutomationResolverBase {
  constructor(
    protected readonly service: CrossPlatformTestingAutomationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformTestingAutomation",
    action: "read",
    possession: "any",
  })
  async _crossPlatformTestingAutomationsMeta(
    @graphql.Args() args: CrossPlatformTestingAutomationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CrossPlatformTestingAutomation])
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformTestingAutomation",
    action: "read",
    possession: "any",
  })
  async crossPlatformTestingAutomations(
    @graphql.Args() args: CrossPlatformTestingAutomationFindManyArgs
  ): Promise<CrossPlatformTestingAutomation[]> {
    return this.service.crossPlatformTestingAutomations(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CrossPlatformTestingAutomation, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformTestingAutomation",
    action: "read",
    possession: "own",
  })
  async crossPlatformTestingAutomation(
    @graphql.Args() args: CrossPlatformTestingAutomationFindUniqueArgs
  ): Promise<CrossPlatformTestingAutomation | null> {
    const result = await this.service.crossPlatformTestingAutomation(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CrossPlatformTestingAutomation)
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformTestingAutomation",
    action: "delete",
    possession: "any",
  })
  async deleteCrossPlatformTestingAutomation(
    @graphql.Args() args: DeleteCrossPlatformTestingAutomationArgs
  ): Promise<CrossPlatformTestingAutomation | null> {
    try {
      return await this.service.deleteCrossPlatformTestingAutomation(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
