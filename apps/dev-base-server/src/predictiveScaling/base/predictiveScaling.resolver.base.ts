/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PredictiveScaling } from "./PredictiveScaling";
import { PredictiveScalingCountArgs } from "./PredictiveScalingCountArgs";
import { PredictiveScalingFindManyArgs } from "./PredictiveScalingFindManyArgs";
import { PredictiveScalingFindUniqueArgs } from "./PredictiveScalingFindUniqueArgs";
import { DeletePredictiveScalingArgs } from "./DeletePredictiveScalingArgs";
import { PredictiveScalingService } from "../predictiveScaling.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PredictiveScaling)
export class PredictiveScalingResolverBase {
  constructor(
    protected readonly service: PredictiveScalingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PredictiveScaling",
    action: "read",
    possession: "any",
  })
  async _predictiveScalingsMeta(
    @graphql.Args() args: PredictiveScalingCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PredictiveScaling])
  @nestAccessControl.UseRoles({
    resource: "PredictiveScaling",
    action: "read",
    possession: "any",
  })
  async predictiveScalings(
    @graphql.Args() args: PredictiveScalingFindManyArgs
  ): Promise<PredictiveScaling[]> {
    return this.service.predictiveScalings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PredictiveScaling, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PredictiveScaling",
    action: "read",
    possession: "own",
  })
  async predictiveScaling(
    @graphql.Args() args: PredictiveScalingFindUniqueArgs
  ): Promise<PredictiveScaling | null> {
    const result = await this.service.predictiveScaling(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PredictiveScaling)
  @nestAccessControl.UseRoles({
    resource: "PredictiveScaling",
    action: "delete",
    possession: "any",
  })
  async deletePredictiveScaling(
    @graphql.Args() args: DeletePredictiveScalingArgs
  ): Promise<PredictiveScaling | null> {
    try {
      return await this.service.deletePredictiveScaling(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
