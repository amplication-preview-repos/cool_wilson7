/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SchemaDesignerService } from "../schemaDesigner.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SchemaDesignerCreateInput } from "./SchemaDesignerCreateInput";
import { SchemaDesigner } from "./SchemaDesigner";
import { SchemaDesignerFindManyArgs } from "./SchemaDesignerFindManyArgs";
import { SchemaDesignerWhereUniqueInput } from "./SchemaDesignerWhereUniqueInput";
import { SchemaDesignerUpdateInput } from "./SchemaDesignerUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SchemaDesignerControllerBase {
  constructor(
    protected readonly service: SchemaDesignerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SchemaDesigner })
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: SchemaDesignerCreateInput,
  })
  async createSchemaDesigner(
    @common.Body() data: SchemaDesignerCreateInput
  ): Promise<SchemaDesigner> {
    return await this.service.createSchemaDesigner({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [SchemaDesigner] })
  @ApiNestedQuery(SchemaDesignerFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async schemaDesigners(
    @common.Req() request: Request
  ): Promise<SchemaDesigner[]> {
    const args = plainToClass(SchemaDesignerFindManyArgs, request.query);
    return this.service.schemaDesigners({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SchemaDesigner })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async schemaDesigner(
    @common.Param() params: SchemaDesignerWhereUniqueInput
  ): Promise<SchemaDesigner | null> {
    const result = await this.service.schemaDesigner({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SchemaDesigner })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: SchemaDesignerUpdateInput,
  })
  async updateSchemaDesigner(
    @common.Param() params: SchemaDesignerWhereUniqueInput,
    @common.Body() data: SchemaDesignerUpdateInput
  ): Promise<SchemaDesigner | null> {
    try {
      return await this.service.updateSchemaDesigner({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SchemaDesigner })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSchemaDesigner(
    @common.Param() params: SchemaDesignerWhereUniqueInput
  ): Promise<SchemaDesigner | null> {
    try {
      return await this.service.deleteSchemaDesigner({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
