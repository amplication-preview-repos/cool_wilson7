/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SchemaDesigner } from "./SchemaDesigner";
import { SchemaDesignerCountArgs } from "./SchemaDesignerCountArgs";
import { SchemaDesignerFindManyArgs } from "./SchemaDesignerFindManyArgs";
import { SchemaDesignerFindUniqueArgs } from "./SchemaDesignerFindUniqueArgs";
import { DeleteSchemaDesignerArgs } from "./DeleteSchemaDesignerArgs";
import { SchemaDesignerService } from "../schemaDesigner.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SchemaDesigner)
export class SchemaDesignerResolverBase {
  constructor(
    protected readonly service: SchemaDesignerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "read",
    possession: "any",
  })
  async _schemaDesignersMeta(
    @graphql.Args() args: SchemaDesignerCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SchemaDesigner])
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "read",
    possession: "any",
  })
  async schemaDesigners(
    @graphql.Args() args: SchemaDesignerFindManyArgs
  ): Promise<SchemaDesigner[]> {
    return this.service.schemaDesigners(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SchemaDesigner, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "read",
    possession: "own",
  })
  async schemaDesigner(
    @graphql.Args() args: SchemaDesignerFindUniqueArgs
  ): Promise<SchemaDesigner | null> {
    const result = await this.service.schemaDesigner(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => SchemaDesigner)
  @nestAccessControl.UseRoles({
    resource: "SchemaDesigner",
    action: "delete",
    possession: "any",
  })
  async deleteSchemaDesigner(
    @graphql.Args() args: DeleteSchemaDesignerArgs
  ): Promise<SchemaDesigner | null> {
    try {
      return await this.service.deleteSchemaDesigner(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
