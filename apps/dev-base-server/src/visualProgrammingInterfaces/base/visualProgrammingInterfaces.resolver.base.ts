/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { VisualProgrammingInterfaces } from "./VisualProgrammingInterfaces";
import { VisualProgrammingInterfacesCountArgs } from "./VisualProgrammingInterfacesCountArgs";
import { VisualProgrammingInterfacesFindManyArgs } from "./VisualProgrammingInterfacesFindManyArgs";
import { VisualProgrammingInterfacesFindUniqueArgs } from "./VisualProgrammingInterfacesFindUniqueArgs";
import { DeleteVisualProgrammingInterfacesArgs } from "./DeleteVisualProgrammingInterfacesArgs";
import { VisualProgrammingInterfacesService } from "../visualProgrammingInterfaces.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => VisualProgrammingInterfaces)
export class VisualProgrammingInterfacesResolverBase {
  constructor(
    protected readonly service: VisualProgrammingInterfacesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "VisualProgrammingInterfaces",
    action: "read",
    possession: "any",
  })
  async _visualProgrammingInterfacesItemsMeta(
    @graphql.Args() args: VisualProgrammingInterfacesCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [VisualProgrammingInterfaces])
  @nestAccessControl.UseRoles({
    resource: "VisualProgrammingInterfaces",
    action: "read",
    possession: "any",
  })
  async visualProgrammingInterfacesItems(
    @graphql.Args() args: VisualProgrammingInterfacesFindManyArgs
  ): Promise<VisualProgrammingInterfaces[]> {
    return this.service.visualProgrammingInterfacesItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => VisualProgrammingInterfaces, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "VisualProgrammingInterfaces",
    action: "read",
    possession: "own",
  })
  async visualProgrammingInterfaces(
    @graphql.Args() args: VisualProgrammingInterfacesFindUniqueArgs
  ): Promise<VisualProgrammingInterfaces | null> {
    const result = await this.service.visualProgrammingInterfaces(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => VisualProgrammingInterfaces)
  @nestAccessControl.UseRoles({
    resource: "VisualProgrammingInterfaces",
    action: "delete",
    possession: "any",
  })
  async deleteVisualProgrammingInterfaces(
    @graphql.Args() args: DeleteVisualProgrammingInterfacesArgs
  ): Promise<VisualProgrammingInterfaces | null> {
    try {
      return await this.service.deleteVisualProgrammingInterfaces(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
