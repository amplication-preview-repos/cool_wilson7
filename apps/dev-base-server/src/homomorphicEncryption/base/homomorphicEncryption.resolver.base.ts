/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { HomomorphicEncryption } from "./HomomorphicEncryption";
import { HomomorphicEncryptionCountArgs } from "./HomomorphicEncryptionCountArgs";
import { HomomorphicEncryptionFindManyArgs } from "./HomomorphicEncryptionFindManyArgs";
import { HomomorphicEncryptionFindUniqueArgs } from "./HomomorphicEncryptionFindUniqueArgs";
import { DeleteHomomorphicEncryptionArgs } from "./DeleteHomomorphicEncryptionArgs";
import { HomomorphicEncryptionService } from "../homomorphicEncryption.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => HomomorphicEncryption)
export class HomomorphicEncryptionResolverBase {
  constructor(
    protected readonly service: HomomorphicEncryptionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "HomomorphicEncryption",
    action: "read",
    possession: "any",
  })
  async _homomorphicEncryptionsMeta(
    @graphql.Args() args: HomomorphicEncryptionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [HomomorphicEncryption])
  @nestAccessControl.UseRoles({
    resource: "HomomorphicEncryption",
    action: "read",
    possession: "any",
  })
  async homomorphicEncryptions(
    @graphql.Args() args: HomomorphicEncryptionFindManyArgs
  ): Promise<HomomorphicEncryption[]> {
    return this.service.homomorphicEncryptions(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => HomomorphicEncryption, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "HomomorphicEncryption",
    action: "read",
    possession: "own",
  })
  async homomorphicEncryption(
    @graphql.Args() args: HomomorphicEncryptionFindUniqueArgs
  ): Promise<HomomorphicEncryption | null> {
    const result = await this.service.homomorphicEncryption(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => HomomorphicEncryption)
  @nestAccessControl.UseRoles({
    resource: "HomomorphicEncryption",
    action: "delete",
    possession: "any",
  })
  async deleteHomomorphicEncryption(
    @graphql.Args() args: DeleteHomomorphicEncryptionArgs
  ): Promise<HomomorphicEncryption | null> {
    try {
      return await this.service.deleteHomomorphicEncryption(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
