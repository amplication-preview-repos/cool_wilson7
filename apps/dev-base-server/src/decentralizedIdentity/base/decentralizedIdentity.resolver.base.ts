/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DecentralizedIdentity } from "./DecentralizedIdentity";
import { DecentralizedIdentityCountArgs } from "./DecentralizedIdentityCountArgs";
import { DecentralizedIdentityFindManyArgs } from "./DecentralizedIdentityFindManyArgs";
import { DecentralizedIdentityFindUniqueArgs } from "./DecentralizedIdentityFindUniqueArgs";
import { DeleteDecentralizedIdentityArgs } from "./DeleteDecentralizedIdentityArgs";
import { DecentralizedIdentityService } from "../decentralizedIdentity.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DecentralizedIdentity)
export class DecentralizedIdentityResolverBase {
  constructor(
    protected readonly service: DecentralizedIdentityService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DecentralizedIdentity",
    action: "read",
    possession: "any",
  })
  async _decentralizedIdentitiesMeta(
    @graphql.Args() args: DecentralizedIdentityCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DecentralizedIdentity])
  @nestAccessControl.UseRoles({
    resource: "DecentralizedIdentity",
    action: "read",
    possession: "any",
  })
  async decentralizedIdentities(
    @graphql.Args() args: DecentralizedIdentityFindManyArgs
  ): Promise<DecentralizedIdentity[]> {
    return this.service.decentralizedIdentities(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DecentralizedIdentity, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DecentralizedIdentity",
    action: "read",
    possession: "own",
  })
  async decentralizedIdentity(
    @graphql.Args() args: DecentralizedIdentityFindUniqueArgs
  ): Promise<DecentralizedIdentity | null> {
    const result = await this.service.decentralizedIdentity(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => DecentralizedIdentity)
  @nestAccessControl.UseRoles({
    resource: "DecentralizedIdentity",
    action: "delete",
    possession: "any",
  })
  async deleteDecentralizedIdentity(
    @graphql.Args() args: DeleteDecentralizedIdentityArgs
  ): Promise<DecentralizedIdentity | null> {
    try {
      return await this.service.deleteDecentralizedIdentity(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
