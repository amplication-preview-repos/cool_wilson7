/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ComplexEventProcessing } from "./ComplexEventProcessing";
import { ComplexEventProcessingCountArgs } from "./ComplexEventProcessingCountArgs";
import { ComplexEventProcessingFindManyArgs } from "./ComplexEventProcessingFindManyArgs";
import { ComplexEventProcessingFindUniqueArgs } from "./ComplexEventProcessingFindUniqueArgs";
import { DeleteComplexEventProcessingArgs } from "./DeleteComplexEventProcessingArgs";
import { ComplexEventProcessingService } from "../complexEventProcessing.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ComplexEventProcessing)
export class ComplexEventProcessingResolverBase {
  constructor(
    protected readonly service: ComplexEventProcessingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ComplexEventProcessing",
    action: "read",
    possession: "any",
  })
  async _complexEventProcessingsMeta(
    @graphql.Args() args: ComplexEventProcessingCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ComplexEventProcessing])
  @nestAccessControl.UseRoles({
    resource: "ComplexEventProcessing",
    action: "read",
    possession: "any",
  })
  async complexEventProcessings(
    @graphql.Args() args: ComplexEventProcessingFindManyArgs
  ): Promise<ComplexEventProcessing[]> {
    return this.service.complexEventProcessings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ComplexEventProcessing, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ComplexEventProcessing",
    action: "read",
    possession: "own",
  })
  async complexEventProcessing(
    @graphql.Args() args: ComplexEventProcessingFindUniqueArgs
  ): Promise<ComplexEventProcessing | null> {
    const result = await this.service.complexEventProcessing(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ComplexEventProcessing)
  @nestAccessControl.UseRoles({
    resource: "ComplexEventProcessing",
    action: "delete",
    possession: "any",
  })
  async deleteComplexEventProcessing(
    @graphql.Args() args: DeleteComplexEventProcessingArgs
  ): Promise<ComplexEventProcessing | null> {
    try {
      return await this.service.deleteComplexEventProcessing(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
