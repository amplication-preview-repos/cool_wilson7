/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ChaosEngineeringTools } from "./ChaosEngineeringTools";
import { ChaosEngineeringToolsCountArgs } from "./ChaosEngineeringToolsCountArgs";
import { ChaosEngineeringToolsFindManyArgs } from "./ChaosEngineeringToolsFindManyArgs";
import { ChaosEngineeringToolsFindUniqueArgs } from "./ChaosEngineeringToolsFindUniqueArgs";
import { DeleteChaosEngineeringToolsArgs } from "./DeleteChaosEngineeringToolsArgs";
import { ChaosEngineeringToolsService } from "../chaosEngineeringTools.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ChaosEngineeringTools)
export class ChaosEngineeringToolsResolverBase {
  constructor(
    protected readonly service: ChaosEngineeringToolsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ChaosEngineeringTools",
    action: "read",
    possession: "any",
  })
  async _chaosEngineeringToolsItemsMeta(
    @graphql.Args() args: ChaosEngineeringToolsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ChaosEngineeringTools])
  @nestAccessControl.UseRoles({
    resource: "ChaosEngineeringTools",
    action: "read",
    possession: "any",
  })
  async chaosEngineeringToolsItems(
    @graphql.Args() args: ChaosEngineeringToolsFindManyArgs
  ): Promise<ChaosEngineeringTools[]> {
    return this.service.chaosEngineeringToolsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ChaosEngineeringTools, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ChaosEngineeringTools",
    action: "read",
    possession: "own",
  })
  async chaosEngineeringTools(
    @graphql.Args() args: ChaosEngineeringToolsFindUniqueArgs
  ): Promise<ChaosEngineeringTools | null> {
    const result = await this.service.chaosEngineeringTools(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ChaosEngineeringTools)
  @nestAccessControl.UseRoles({
    resource: "ChaosEngineeringTools",
    action: "delete",
    possession: "any",
  })
  async deleteChaosEngineeringTools(
    @graphql.Args() args: DeleteChaosEngineeringToolsArgs
  ): Promise<ChaosEngineeringTools | null> {
    try {
      return await this.service.deleteChaosEngineeringTools(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
