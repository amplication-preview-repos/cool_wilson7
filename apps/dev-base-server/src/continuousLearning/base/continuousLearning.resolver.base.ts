/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ContinuousLearning } from "./ContinuousLearning";
import { ContinuousLearningCountArgs } from "./ContinuousLearningCountArgs";
import { ContinuousLearningFindManyArgs } from "./ContinuousLearningFindManyArgs";
import { ContinuousLearningFindUniqueArgs } from "./ContinuousLearningFindUniqueArgs";
import { DeleteContinuousLearningArgs } from "./DeleteContinuousLearningArgs";
import { ContinuousLearningService } from "../continuousLearning.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ContinuousLearning)
export class ContinuousLearningResolverBase {
  constructor(
    protected readonly service: ContinuousLearningService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ContinuousLearning",
    action: "read",
    possession: "any",
  })
  async _continuousLearningsMeta(
    @graphql.Args() args: ContinuousLearningCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ContinuousLearning])
  @nestAccessControl.UseRoles({
    resource: "ContinuousLearning",
    action: "read",
    possession: "any",
  })
  async continuousLearnings(
    @graphql.Args() args: ContinuousLearningFindManyArgs
  ): Promise<ContinuousLearning[]> {
    return this.service.continuousLearnings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ContinuousLearning, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ContinuousLearning",
    action: "read",
    possession: "own",
  })
  async continuousLearning(
    @graphql.Args() args: ContinuousLearningFindUniqueArgs
  ): Promise<ContinuousLearning | null> {
    const result = await this.service.continuousLearning(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ContinuousLearning)
  @nestAccessControl.UseRoles({
    resource: "ContinuousLearning",
    action: "delete",
    possession: "any",
  })
  async deleteContinuousLearning(
    @graphql.Args() args: DeleteContinuousLearningArgs
  ): Promise<ContinuousLearning | null> {
    try {
      return await this.service.deleteContinuousLearning(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
