/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AnomalyDetection } from "./AnomalyDetection";
import { AnomalyDetectionCountArgs } from "./AnomalyDetectionCountArgs";
import { AnomalyDetectionFindManyArgs } from "./AnomalyDetectionFindManyArgs";
import { AnomalyDetectionFindUniqueArgs } from "./AnomalyDetectionFindUniqueArgs";
import { DeleteAnomalyDetectionArgs } from "./DeleteAnomalyDetectionArgs";
import { AnomalyDetectionService } from "../anomalyDetection.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => AnomalyDetection)
export class AnomalyDetectionResolverBase {
  constructor(
    protected readonly service: AnomalyDetectionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "AnomalyDetection",
    action: "read",
    possession: "any",
  })
  async _anomalyDetectionsMeta(
    @graphql.Args() args: AnomalyDetectionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [AnomalyDetection])
  @nestAccessControl.UseRoles({
    resource: "AnomalyDetection",
    action: "read",
    possession: "any",
  })
  async anomalyDetections(
    @graphql.Args() args: AnomalyDetectionFindManyArgs
  ): Promise<AnomalyDetection[]> {
    return this.service.anomalyDetections(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => AnomalyDetection, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "AnomalyDetection",
    action: "read",
    possession: "own",
  })
  async anomalyDetection(
    @graphql.Args() args: AnomalyDetectionFindUniqueArgs
  ): Promise<AnomalyDetection | null> {
    const result = await this.service.anomalyDetection(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => AnomalyDetection)
  @nestAccessControl.UseRoles({
    resource: "AnomalyDetection",
    action: "delete",
    possession: "any",
  })
  async deleteAnomalyDetection(
    @graphql.Args() args: DeleteAnomalyDetectionArgs
  ): Promise<AnomalyDetection | null> {
    try {
      return await this.service.deleteAnomalyDetection(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
