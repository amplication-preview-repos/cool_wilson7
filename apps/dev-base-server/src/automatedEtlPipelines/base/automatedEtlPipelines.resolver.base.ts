/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AutomatedEtlPipelines } from "./AutomatedEtlPipelines";
import { AutomatedEtlPipelinesCountArgs } from "./AutomatedEtlPipelinesCountArgs";
import { AutomatedEtlPipelinesFindManyArgs } from "./AutomatedEtlPipelinesFindManyArgs";
import { AutomatedEtlPipelinesFindUniqueArgs } from "./AutomatedEtlPipelinesFindUniqueArgs";
import { DeleteAutomatedEtlPipelinesArgs } from "./DeleteAutomatedEtlPipelinesArgs";
import { AutomatedEtlPipelinesService } from "../automatedEtlPipelines.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => AutomatedEtlPipelines)
export class AutomatedEtlPipelinesResolverBase {
  constructor(
    protected readonly service: AutomatedEtlPipelinesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "AutomatedEtlPipelines",
    action: "read",
    possession: "any",
  })
  async _automatedEtlPipelinesItemsMeta(
    @graphql.Args() args: AutomatedEtlPipelinesCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [AutomatedEtlPipelines])
  @nestAccessControl.UseRoles({
    resource: "AutomatedEtlPipelines",
    action: "read",
    possession: "any",
  })
  async automatedEtlPipelinesItems(
    @graphql.Args() args: AutomatedEtlPipelinesFindManyArgs
  ): Promise<AutomatedEtlPipelines[]> {
    return this.service.automatedEtlPipelinesItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => AutomatedEtlPipelines, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "AutomatedEtlPipelines",
    action: "read",
    possession: "own",
  })
  async automatedEtlPipelines(
    @graphql.Args() args: AutomatedEtlPipelinesFindUniqueArgs
  ): Promise<AutomatedEtlPipelines | null> {
    const result = await this.service.automatedEtlPipelines(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => AutomatedEtlPipelines)
  @nestAccessControl.UseRoles({
    resource: "AutomatedEtlPipelines",
    action: "delete",
    possession: "any",
  })
  async deleteAutomatedEtlPipelines(
    @graphql.Args() args: DeleteAutomatedEtlPipelinesArgs
  ): Promise<AutomatedEtlPipelines | null> {
    try {
      return await this.service.deleteAutomatedEtlPipelines(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
