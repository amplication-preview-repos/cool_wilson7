/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ContextAwareHelp } from "./ContextAwareHelp";
import { ContextAwareHelpCountArgs } from "./ContextAwareHelpCountArgs";
import { ContextAwareHelpFindManyArgs } from "./ContextAwareHelpFindManyArgs";
import { ContextAwareHelpFindUniqueArgs } from "./ContextAwareHelpFindUniqueArgs";
import { DeleteContextAwareHelpArgs } from "./DeleteContextAwareHelpArgs";
import { ContextAwareHelpService } from "../contextAwareHelp.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ContextAwareHelp)
export class ContextAwareHelpResolverBase {
  constructor(
    protected readonly service: ContextAwareHelpService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "read",
    possession: "any",
  })
  async _contextAwareHelpsMeta(
    @graphql.Args() args: ContextAwareHelpCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ContextAwareHelp])
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "read",
    possession: "any",
  })
  async contextAwareHelps(
    @graphql.Args() args: ContextAwareHelpFindManyArgs
  ): Promise<ContextAwareHelp[]> {
    return this.service.contextAwareHelps(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ContextAwareHelp, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "read",
    possession: "own",
  })
  async contextAwareHelp(
    @graphql.Args() args: ContextAwareHelpFindUniqueArgs
  ): Promise<ContextAwareHelp | null> {
    const result = await this.service.contextAwareHelp(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ContextAwareHelp)
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "delete",
    possession: "any",
  })
  async deleteContextAwareHelp(
    @graphql.Args() args: DeleteContextAwareHelpArgs
  ): Promise<ContextAwareHelp | null> {
    try {
      return await this.service.deleteContextAwareHelp(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
