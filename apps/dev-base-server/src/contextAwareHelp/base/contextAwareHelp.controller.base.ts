/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ContextAwareHelpService } from "../contextAwareHelp.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ContextAwareHelpCreateInput } from "./ContextAwareHelpCreateInput";
import { ContextAwareHelp } from "./ContextAwareHelp";
import { ContextAwareHelpFindManyArgs } from "./ContextAwareHelpFindManyArgs";
import { ContextAwareHelpWhereUniqueInput } from "./ContextAwareHelpWhereUniqueInput";
import { ContextAwareHelpUpdateInput } from "./ContextAwareHelpUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ContextAwareHelpControllerBase {
  constructor(
    protected readonly service: ContextAwareHelpService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ContextAwareHelp })
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createContextAwareHelp(
    @common.Body() data: ContextAwareHelpCreateInput
  ): Promise<ContextAwareHelp> {
    return await this.service.createContextAwareHelp({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ContextAwareHelp] })
  @ApiNestedQuery(ContextAwareHelpFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async contextAwareHelps(
    @common.Req() request: Request
  ): Promise<ContextAwareHelp[]> {
    const args = plainToClass(ContextAwareHelpFindManyArgs, request.query);
    return this.service.contextAwareHelps({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ContextAwareHelp })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async contextAwareHelp(
    @common.Param() params: ContextAwareHelpWhereUniqueInput
  ): Promise<ContextAwareHelp | null> {
    const result = await this.service.contextAwareHelp({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ContextAwareHelp })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateContextAwareHelp(
    @common.Param() params: ContextAwareHelpWhereUniqueInput,
    @common.Body() data: ContextAwareHelpUpdateInput
  ): Promise<ContextAwareHelp | null> {
    try {
      return await this.service.updateContextAwareHelp({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ContextAwareHelp })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ContextAwareHelp",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteContextAwareHelp(
    @common.Param() params: ContextAwareHelpWhereUniqueInput
  ): Promise<ContextAwareHelp | null> {
    try {
      return await this.service.deleteContextAwareHelp({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
