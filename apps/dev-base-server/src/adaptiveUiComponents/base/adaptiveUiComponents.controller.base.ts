/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { AdaptiveUiComponentsService } from "../adaptiveUiComponents.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AdaptiveUiComponentsCreateInput } from "./AdaptiveUiComponentsCreateInput";
import { AdaptiveUiComponents } from "./AdaptiveUiComponents";
import { AdaptiveUiComponentsFindManyArgs } from "./AdaptiveUiComponentsFindManyArgs";
import { AdaptiveUiComponentsWhereUniqueInput } from "./AdaptiveUiComponentsWhereUniqueInput";
import { AdaptiveUiComponentsUpdateInput } from "./AdaptiveUiComponentsUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class AdaptiveUiComponentsControllerBase {
  constructor(
    protected readonly service: AdaptiveUiComponentsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: AdaptiveUiComponents })
  @nestAccessControl.UseRoles({
    resource: "AdaptiveUiComponents",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createAdaptiveUiComponents(
    @common.Body() data: AdaptiveUiComponentsCreateInput
  ): Promise<AdaptiveUiComponents> {
    return await this.service.createAdaptiveUiComponents({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [AdaptiveUiComponents] })
  @ApiNestedQuery(AdaptiveUiComponentsFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AdaptiveUiComponents",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async adaptiveUiComponentsItems(
    @common.Req() request: Request
  ): Promise<AdaptiveUiComponents[]> {
    const args = plainToClass(AdaptiveUiComponentsFindManyArgs, request.query);
    return this.service.adaptiveUiComponentsItems({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: AdaptiveUiComponents })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AdaptiveUiComponents",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async adaptiveUiComponents(
    @common.Param() params: AdaptiveUiComponentsWhereUniqueInput
  ): Promise<AdaptiveUiComponents | null> {
    const result = await this.service.adaptiveUiComponents({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: AdaptiveUiComponents })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AdaptiveUiComponents",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateAdaptiveUiComponents(
    @common.Param() params: AdaptiveUiComponentsWhereUniqueInput,
    @common.Body() data: AdaptiveUiComponentsUpdateInput
  ): Promise<AdaptiveUiComponents | null> {
    try {
      return await this.service.updateAdaptiveUiComponents({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: AdaptiveUiComponents })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AdaptiveUiComponents",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteAdaptiveUiComponents(
    @common.Param() params: AdaptiveUiComponentsWhereUniqueInput
  ): Promise<AdaptiveUiComponents | null> {
    try {
      return await this.service.deleteAdaptiveUiComponents({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
