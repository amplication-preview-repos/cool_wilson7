/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CrossPlatformDevelopment } from "./CrossPlatformDevelopment";
import { CrossPlatformDevelopmentCountArgs } from "./CrossPlatformDevelopmentCountArgs";
import { CrossPlatformDevelopmentFindManyArgs } from "./CrossPlatformDevelopmentFindManyArgs";
import { CrossPlatformDevelopmentFindUniqueArgs } from "./CrossPlatformDevelopmentFindUniqueArgs";
import { DeleteCrossPlatformDevelopmentArgs } from "./DeleteCrossPlatformDevelopmentArgs";
import { CrossPlatformDevelopmentService } from "../crossPlatformDevelopment.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CrossPlatformDevelopment)
export class CrossPlatformDevelopmentResolverBase {
  constructor(
    protected readonly service: CrossPlatformDevelopmentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformDevelopment",
    action: "read",
    possession: "any",
  })
  async _crossPlatformDevelopmentsMeta(
    @graphql.Args() args: CrossPlatformDevelopmentCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CrossPlatformDevelopment])
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformDevelopment",
    action: "read",
    possession: "any",
  })
  async crossPlatformDevelopments(
    @graphql.Args() args: CrossPlatformDevelopmentFindManyArgs
  ): Promise<CrossPlatformDevelopment[]> {
    return this.service.crossPlatformDevelopments(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CrossPlatformDevelopment, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformDevelopment",
    action: "read",
    possession: "own",
  })
  async crossPlatformDevelopment(
    @graphql.Args() args: CrossPlatformDevelopmentFindUniqueArgs
  ): Promise<CrossPlatformDevelopment | null> {
    const result = await this.service.crossPlatformDevelopment(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CrossPlatformDevelopment)
  @nestAccessControl.UseRoles({
    resource: "CrossPlatformDevelopment",
    action: "delete",
    possession: "any",
  })
  async deleteCrossPlatformDevelopment(
    @graphql.Args() args: DeleteCrossPlatformDevelopmentArgs
  ): Promise<CrossPlatformDevelopment | null> {
    try {
      return await this.service.deleteCrossPlatformDevelopment(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
