/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EmotionAi } from "./EmotionAi";
import { EmotionAiCountArgs } from "./EmotionAiCountArgs";
import { EmotionAiFindManyArgs } from "./EmotionAiFindManyArgs";
import { EmotionAiFindUniqueArgs } from "./EmotionAiFindUniqueArgs";
import { DeleteEmotionAiArgs } from "./DeleteEmotionAiArgs";
import { EmotionAiService } from "../emotionAi.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => EmotionAi)
export class EmotionAiResolverBase {
  constructor(
    protected readonly service: EmotionAiService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "EmotionAi",
    action: "read",
    possession: "any",
  })
  async _emotionAisMeta(
    @graphql.Args() args: EmotionAiCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [EmotionAi])
  @nestAccessControl.UseRoles({
    resource: "EmotionAi",
    action: "read",
    possession: "any",
  })
  async emotionAis(
    @graphql.Args() args: EmotionAiFindManyArgs
  ): Promise<EmotionAi[]> {
    return this.service.emotionAis(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => EmotionAi, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EmotionAi",
    action: "read",
    possession: "own",
  })
  async emotionAi(
    @graphql.Args() args: EmotionAiFindUniqueArgs
  ): Promise<EmotionAi | null> {
    const result = await this.service.emotionAi(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => EmotionAi)
  @nestAccessControl.UseRoles({
    resource: "EmotionAi",
    action: "delete",
    possession: "any",
  })
  async deleteEmotionAi(
    @graphql.Args() args: DeleteEmotionAiArgs
  ): Promise<EmotionAi | null> {
    try {
      return await this.service.deleteEmotionAi(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
