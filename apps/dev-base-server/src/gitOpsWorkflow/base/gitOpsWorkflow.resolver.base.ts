/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GitOpsWorkflow } from "./GitOpsWorkflow";
import { GitOpsWorkflowCountArgs } from "./GitOpsWorkflowCountArgs";
import { GitOpsWorkflowFindManyArgs } from "./GitOpsWorkflowFindManyArgs";
import { GitOpsWorkflowFindUniqueArgs } from "./GitOpsWorkflowFindUniqueArgs";
import { DeleteGitOpsWorkflowArgs } from "./DeleteGitOpsWorkflowArgs";
import { GitOpsWorkflowService } from "../gitOpsWorkflow.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GitOpsWorkflow)
export class GitOpsWorkflowResolverBase {
  constructor(
    protected readonly service: GitOpsWorkflowService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GitOpsWorkflow",
    action: "read",
    possession: "any",
  })
  async _gitOpsWorkflowsMeta(
    @graphql.Args() args: GitOpsWorkflowCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GitOpsWorkflow])
  @nestAccessControl.UseRoles({
    resource: "GitOpsWorkflow",
    action: "read",
    possession: "any",
  })
  async gitOpsWorkflows(
    @graphql.Args() args: GitOpsWorkflowFindManyArgs
  ): Promise<GitOpsWorkflow[]> {
    return this.service.gitOpsWorkflows(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GitOpsWorkflow, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GitOpsWorkflow",
    action: "read",
    possession: "own",
  })
  async gitOpsWorkflow(
    @graphql.Args() args: GitOpsWorkflowFindUniqueArgs
  ): Promise<GitOpsWorkflow | null> {
    const result = await this.service.gitOpsWorkflow(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GitOpsWorkflow)
  @nestAccessControl.UseRoles({
    resource: "GitOpsWorkflow",
    action: "delete",
    possession: "any",
  })
  async deleteGitOpsWorkflow(
    @graphql.Args() args: DeleteGitOpsWorkflowArgs
  ): Promise<GitOpsWorkflow | null> {
    try {
      return await this.service.deleteGitOpsWorkflow(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
