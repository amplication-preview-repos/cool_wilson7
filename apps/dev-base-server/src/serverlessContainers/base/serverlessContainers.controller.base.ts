/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ServerlessContainersService } from "../serverlessContainers.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ServerlessContainersCreateInput } from "./ServerlessContainersCreateInput";
import { ServerlessContainers } from "./ServerlessContainers";
import { ServerlessContainersFindManyArgs } from "./ServerlessContainersFindManyArgs";
import { ServerlessContainersWhereUniqueInput } from "./ServerlessContainersWhereUniqueInput";
import { ServerlessContainersUpdateInput } from "./ServerlessContainersUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ServerlessContainersControllerBase {
  constructor(
    protected readonly service: ServerlessContainersService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ServerlessContainers })
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createServerlessContainers(
    @common.Body() data: ServerlessContainersCreateInput
  ): Promise<ServerlessContainers> {
    return await this.service.createServerlessContainers({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ServerlessContainers] })
  @ApiNestedQuery(ServerlessContainersFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async serverlessContainersItems(
    @common.Req() request: Request
  ): Promise<ServerlessContainers[]> {
    const args = plainToClass(ServerlessContainersFindManyArgs, request.query);
    return this.service.serverlessContainersItems({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ServerlessContainers })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async serverlessContainers(
    @common.Param() params: ServerlessContainersWhereUniqueInput
  ): Promise<ServerlessContainers | null> {
    const result = await this.service.serverlessContainers({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ServerlessContainers })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateServerlessContainers(
    @common.Param() params: ServerlessContainersWhereUniqueInput,
    @common.Body() data: ServerlessContainersUpdateInput
  ): Promise<ServerlessContainers | null> {
    try {
      return await this.service.updateServerlessContainers({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ServerlessContainers })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteServerlessContainers(
    @common.Param() params: ServerlessContainersWhereUniqueInput
  ): Promise<ServerlessContainers | null> {
    try {
      return await this.service.deleteServerlessContainers({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
