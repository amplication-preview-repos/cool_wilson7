/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ServerlessContainers } from "./ServerlessContainers";
import { ServerlessContainersCountArgs } from "./ServerlessContainersCountArgs";
import { ServerlessContainersFindManyArgs } from "./ServerlessContainersFindManyArgs";
import { ServerlessContainersFindUniqueArgs } from "./ServerlessContainersFindUniqueArgs";
import { DeleteServerlessContainersArgs } from "./DeleteServerlessContainersArgs";
import { ServerlessContainersService } from "../serverlessContainers.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ServerlessContainers)
export class ServerlessContainersResolverBase {
  constructor(
    protected readonly service: ServerlessContainersService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "read",
    possession: "any",
  })
  async _serverlessContainersItemsMeta(
    @graphql.Args() args: ServerlessContainersCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ServerlessContainers])
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "read",
    possession: "any",
  })
  async serverlessContainersItems(
    @graphql.Args() args: ServerlessContainersFindManyArgs
  ): Promise<ServerlessContainers[]> {
    return this.service.serverlessContainersItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ServerlessContainers, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "read",
    possession: "own",
  })
  async serverlessContainers(
    @graphql.Args() args: ServerlessContainersFindUniqueArgs
  ): Promise<ServerlessContainers | null> {
    const result = await this.service.serverlessContainers(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ServerlessContainers)
  @nestAccessControl.UseRoles({
    resource: "ServerlessContainers",
    action: "delete",
    possession: "any",
  })
  async deleteServerlessContainers(
    @graphql.Args() args: DeleteServerlessContainersArgs
  ): Promise<ServerlessContainers | null> {
    try {
      return await this.service.deleteServerlessContainers(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
