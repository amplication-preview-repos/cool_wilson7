/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SmartContractDevelopmentService } from "../smartContractDevelopment.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SmartContractDevelopmentCreateInput } from "./SmartContractDevelopmentCreateInput";
import { SmartContractDevelopment } from "./SmartContractDevelopment";
import { SmartContractDevelopmentFindManyArgs } from "./SmartContractDevelopmentFindManyArgs";
import { SmartContractDevelopmentWhereUniqueInput } from "./SmartContractDevelopmentWhereUniqueInput";
import { SmartContractDevelopmentUpdateInput } from "./SmartContractDevelopmentUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SmartContractDevelopmentControllerBase {
  constructor(
    protected readonly service: SmartContractDevelopmentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: SmartContractDevelopment })
  @nestAccessControl.UseRoles({
    resource: "SmartContractDevelopment",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createSmartContractDevelopment(
    @common.Body() data: SmartContractDevelopmentCreateInput
  ): Promise<SmartContractDevelopment> {
    return await this.service.createSmartContractDevelopment({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [SmartContractDevelopment] })
  @ApiNestedQuery(SmartContractDevelopmentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SmartContractDevelopment",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async smartContractDevelopments(
    @common.Req() request: Request
  ): Promise<SmartContractDevelopment[]> {
    const args = plainToClass(
      SmartContractDevelopmentFindManyArgs,
      request.query
    );
    return this.service.smartContractDevelopments({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: SmartContractDevelopment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SmartContractDevelopment",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async smartContractDevelopment(
    @common.Param() params: SmartContractDevelopmentWhereUniqueInput
  ): Promise<SmartContractDevelopment | null> {
    const result = await this.service.smartContractDevelopment({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: SmartContractDevelopment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SmartContractDevelopment",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateSmartContractDevelopment(
    @common.Param() params: SmartContractDevelopmentWhereUniqueInput,
    @common.Body() data: SmartContractDevelopmentUpdateInput
  ): Promise<SmartContractDevelopment | null> {
    try {
      return await this.service.updateSmartContractDevelopment({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: SmartContractDevelopment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "SmartContractDevelopment",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSmartContractDevelopment(
    @common.Param() params: SmartContractDevelopmentWhereUniqueInput
  ): Promise<SmartContractDevelopment | null> {
    try {
      return await this.service.deleteSmartContractDevelopment({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
