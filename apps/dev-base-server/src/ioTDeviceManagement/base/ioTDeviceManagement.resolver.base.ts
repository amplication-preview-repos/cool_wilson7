/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { IoTDeviceManagement } from "./IoTDeviceManagement";
import { IoTDeviceManagementCountArgs } from "./IoTDeviceManagementCountArgs";
import { IoTDeviceManagementFindManyArgs } from "./IoTDeviceManagementFindManyArgs";
import { IoTDeviceManagementFindUniqueArgs } from "./IoTDeviceManagementFindUniqueArgs";
import { DeleteIoTDeviceManagementArgs } from "./DeleteIoTDeviceManagementArgs";
import { IoTDeviceManagementService } from "../ioTDeviceManagement.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => IoTDeviceManagement)
export class IoTDeviceManagementResolverBase {
  constructor(
    protected readonly service: IoTDeviceManagementService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "IoTDeviceManagement",
    action: "read",
    possession: "any",
  })
  async _ioTDeviceManagementsMeta(
    @graphql.Args() args: IoTDeviceManagementCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [IoTDeviceManagement])
  @nestAccessControl.UseRoles({
    resource: "IoTDeviceManagement",
    action: "read",
    possession: "any",
  })
  async ioTDeviceManagements(
    @graphql.Args() args: IoTDeviceManagementFindManyArgs
  ): Promise<IoTDeviceManagement[]> {
    return this.service.ioTDeviceManagements(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => IoTDeviceManagement, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "IoTDeviceManagement",
    action: "read",
    possession: "own",
  })
  async ioTDeviceManagement(
    @graphql.Args() args: IoTDeviceManagementFindUniqueArgs
  ): Promise<IoTDeviceManagement | null> {
    const result = await this.service.ioTDeviceManagement(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => IoTDeviceManagement)
  @nestAccessControl.UseRoles({
    resource: "IoTDeviceManagement",
    action: "delete",
    possession: "any",
  })
  async deleteIoTDeviceManagement(
    @graphql.Args() args: DeleteIoTDeviceManagementArgs
  ): Promise<IoTDeviceManagement | null> {
    try {
      return await this.service.deleteIoTDeviceManagement(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
