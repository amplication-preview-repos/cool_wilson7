/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EnergyEfficientCoding } from "./EnergyEfficientCoding";
import { EnergyEfficientCodingCountArgs } from "./EnergyEfficientCodingCountArgs";
import { EnergyEfficientCodingFindManyArgs } from "./EnergyEfficientCodingFindManyArgs";
import { EnergyEfficientCodingFindUniqueArgs } from "./EnergyEfficientCodingFindUniqueArgs";
import { DeleteEnergyEfficientCodingArgs } from "./DeleteEnergyEfficientCodingArgs";
import { EnergyEfficientCodingService } from "../energyEfficientCoding.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => EnergyEfficientCoding)
export class EnergyEfficientCodingResolverBase {
  constructor(
    protected readonly service: EnergyEfficientCodingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "EnergyEfficientCoding",
    action: "read",
    possession: "any",
  })
  async _energyEfficientCodingsMeta(
    @graphql.Args() args: EnergyEfficientCodingCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [EnergyEfficientCoding])
  @nestAccessControl.UseRoles({
    resource: "EnergyEfficientCoding",
    action: "read",
    possession: "any",
  })
  async energyEfficientCodings(
    @graphql.Args() args: EnergyEfficientCodingFindManyArgs
  ): Promise<EnergyEfficientCoding[]> {
    return this.service.energyEfficientCodings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => EnergyEfficientCoding, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "EnergyEfficientCoding",
    action: "read",
    possession: "own",
  })
  async energyEfficientCoding(
    @graphql.Args() args: EnergyEfficientCodingFindUniqueArgs
  ): Promise<EnergyEfficientCoding | null> {
    const result = await this.service.energyEfficientCoding(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => EnergyEfficientCoding)
  @nestAccessControl.UseRoles({
    resource: "EnergyEfficientCoding",
    action: "delete",
    possession: "any",
  })
  async deleteEnergyEfficientCoding(
    @graphql.Args() args: DeleteEnergyEfficientCodingArgs
  ): Promise<EnergyEfficientCoding | null> {
    try {
      return await this.service.deleteEnergyEfficientCoding(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
