/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DataRetentionPolicies } from "./DataRetentionPolicies";
import { DataRetentionPoliciesCountArgs } from "./DataRetentionPoliciesCountArgs";
import { DataRetentionPoliciesFindManyArgs } from "./DataRetentionPoliciesFindManyArgs";
import { DataRetentionPoliciesFindUniqueArgs } from "./DataRetentionPoliciesFindUniqueArgs";
import { DeleteDataRetentionPoliciesArgs } from "./DeleteDataRetentionPoliciesArgs";
import { DataRetentionPoliciesService } from "../dataRetentionPolicies.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DataRetentionPolicies)
export class DataRetentionPoliciesResolverBase {
  constructor(
    protected readonly service: DataRetentionPoliciesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DataRetentionPolicies",
    action: "read",
    possession: "any",
  })
  async _dataRetentionPoliciesItemsMeta(
    @graphql.Args() args: DataRetentionPoliciesCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DataRetentionPolicies])
  @nestAccessControl.UseRoles({
    resource: "DataRetentionPolicies",
    action: "read",
    possession: "any",
  })
  async dataRetentionPoliciesItems(
    @graphql.Args() args: DataRetentionPoliciesFindManyArgs
  ): Promise<DataRetentionPolicies[]> {
    return this.service.dataRetentionPoliciesItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DataRetentionPolicies, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DataRetentionPolicies",
    action: "read",
    possession: "own",
  })
  async dataRetentionPolicies(
    @graphql.Args() args: DataRetentionPoliciesFindUniqueArgs
  ): Promise<DataRetentionPolicies | null> {
    const result = await this.service.dataRetentionPolicies(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => DataRetentionPolicies)
  @nestAccessControl.UseRoles({
    resource: "DataRetentionPolicies",
    action: "delete",
    possession: "any",
  })
  async deleteDataRetentionPolicies(
    @graphql.Args() args: DeleteDataRetentionPoliciesArgs
  ): Promise<DataRetentionPolicies | null> {
    try {
      return await this.service.deleteDataRetentionPolicies(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
