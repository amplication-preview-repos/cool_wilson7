/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { StreamProcessing } from "./StreamProcessing";
import { StreamProcessingCountArgs } from "./StreamProcessingCountArgs";
import { StreamProcessingFindManyArgs } from "./StreamProcessingFindManyArgs";
import { StreamProcessingFindUniqueArgs } from "./StreamProcessingFindUniqueArgs";
import { DeleteStreamProcessingArgs } from "./DeleteStreamProcessingArgs";
import { StreamProcessingService } from "../streamProcessing.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => StreamProcessing)
export class StreamProcessingResolverBase {
  constructor(
    protected readonly service: StreamProcessingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "StreamProcessing",
    action: "read",
    possession: "any",
  })
  async _streamProcessingsMeta(
    @graphql.Args() args: StreamProcessingCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [StreamProcessing])
  @nestAccessControl.UseRoles({
    resource: "StreamProcessing",
    action: "read",
    possession: "any",
  })
  async streamProcessings(
    @graphql.Args() args: StreamProcessingFindManyArgs
  ): Promise<StreamProcessing[]> {
    return this.service.streamProcessings(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => StreamProcessing, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "StreamProcessing",
    action: "read",
    possession: "own",
  })
  async streamProcessing(
    @graphql.Args() args: StreamProcessingFindUniqueArgs
  ): Promise<StreamProcessing | null> {
    const result = await this.service.streamProcessing(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => StreamProcessing)
  @nestAccessControl.UseRoles({
    resource: "StreamProcessing",
    action: "delete",
    possession: "any",
  })
  async deleteStreamProcessing(
    @graphql.Args() args: DeleteStreamProcessingArgs
  ): Promise<StreamProcessing | null> {
    try {
      return await this.service.deleteStreamProcessing(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
