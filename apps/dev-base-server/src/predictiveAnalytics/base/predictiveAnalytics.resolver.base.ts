/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PredictiveAnalytics } from "./PredictiveAnalytics";
import { PredictiveAnalyticsCountArgs } from "./PredictiveAnalyticsCountArgs";
import { PredictiveAnalyticsFindManyArgs } from "./PredictiveAnalyticsFindManyArgs";
import { PredictiveAnalyticsFindUniqueArgs } from "./PredictiveAnalyticsFindUniqueArgs";
import { DeletePredictiveAnalyticsArgs } from "./DeletePredictiveAnalyticsArgs";
import { PredictiveAnalyticsService } from "../predictiveAnalytics.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PredictiveAnalytics)
export class PredictiveAnalyticsResolverBase {
  constructor(
    protected readonly service: PredictiveAnalyticsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PredictiveAnalytics",
    action: "read",
    possession: "any",
  })
  async _predictiveAnalyticsItemsMeta(
    @graphql.Args() args: PredictiveAnalyticsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PredictiveAnalytics])
  @nestAccessControl.UseRoles({
    resource: "PredictiveAnalytics",
    action: "read",
    possession: "any",
  })
  async predictiveAnalyticsItems(
    @graphql.Args() args: PredictiveAnalyticsFindManyArgs
  ): Promise<PredictiveAnalytics[]> {
    return this.service.predictiveAnalyticsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PredictiveAnalytics, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PredictiveAnalytics",
    action: "read",
    possession: "own",
  })
  async predictiveAnalytics(
    @graphql.Args() args: PredictiveAnalyticsFindUniqueArgs
  ): Promise<PredictiveAnalytics | null> {
    const result = await this.service.predictiveAnalytics(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PredictiveAnalytics)
  @nestAccessControl.UseRoles({
    resource: "PredictiveAnalytics",
    action: "delete",
    possession: "any",
  })
  async deletePredictiveAnalytics(
    @graphql.Args() args: DeletePredictiveAnalyticsArgs
  ): Promise<PredictiveAnalytics | null> {
    try {
      return await this.service.deletePredictiveAnalytics(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
