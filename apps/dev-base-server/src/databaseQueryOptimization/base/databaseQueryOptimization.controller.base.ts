/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DatabaseQueryOptimizationService } from "../databaseQueryOptimization.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DatabaseQueryOptimizationCreateInput } from "./DatabaseQueryOptimizationCreateInput";
import { DatabaseQueryOptimization } from "./DatabaseQueryOptimization";
import { DatabaseQueryOptimizationFindManyArgs } from "./DatabaseQueryOptimizationFindManyArgs";
import { DatabaseQueryOptimizationWhereUniqueInput } from "./DatabaseQueryOptimizationWhereUniqueInput";
import { DatabaseQueryOptimizationUpdateInput } from "./DatabaseQueryOptimizationUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DatabaseQueryOptimizationControllerBase {
  constructor(
    protected readonly service: DatabaseQueryOptimizationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DatabaseQueryOptimization })
  @nestAccessControl.UseRoles({
    resource: "DatabaseQueryOptimization",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDatabaseQueryOptimization(
    @common.Body() data: DatabaseQueryOptimizationCreateInput
  ): Promise<DatabaseQueryOptimization> {
    return await this.service.createDatabaseQueryOptimization({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DatabaseQueryOptimization] })
  @ApiNestedQuery(DatabaseQueryOptimizationFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DatabaseQueryOptimization",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async databaseQueryOptimizations(
    @common.Req() request: Request
  ): Promise<DatabaseQueryOptimization[]> {
    const args = plainToClass(
      DatabaseQueryOptimizationFindManyArgs,
      request.query
    );
    return this.service.databaseQueryOptimizations({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DatabaseQueryOptimization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DatabaseQueryOptimization",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async databaseQueryOptimization(
    @common.Param() params: DatabaseQueryOptimizationWhereUniqueInput
  ): Promise<DatabaseQueryOptimization | null> {
    const result = await this.service.databaseQueryOptimization({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DatabaseQueryOptimization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DatabaseQueryOptimization",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDatabaseQueryOptimization(
    @common.Param() params: DatabaseQueryOptimizationWhereUniqueInput,
    @common.Body() data: DatabaseQueryOptimizationUpdateInput
  ): Promise<DatabaseQueryOptimization | null> {
    try {
      return await this.service.updateDatabaseQueryOptimization({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DatabaseQueryOptimization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DatabaseQueryOptimization",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDatabaseQueryOptimization(
    @common.Param() params: DatabaseQueryOptimizationWhereUniqueInput
  ): Promise<DatabaseQueryOptimization | null> {
    try {
      return await this.service.deleteDatabaseQueryOptimization({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
