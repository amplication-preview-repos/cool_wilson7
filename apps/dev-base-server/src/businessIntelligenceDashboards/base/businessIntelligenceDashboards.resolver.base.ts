/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BusinessIntelligenceDashboards } from "./BusinessIntelligenceDashboards";
import { BusinessIntelligenceDashboardsCountArgs } from "./BusinessIntelligenceDashboardsCountArgs";
import { BusinessIntelligenceDashboardsFindManyArgs } from "./BusinessIntelligenceDashboardsFindManyArgs";
import { BusinessIntelligenceDashboardsFindUniqueArgs } from "./BusinessIntelligenceDashboardsFindUniqueArgs";
import { DeleteBusinessIntelligenceDashboardsArgs } from "./DeleteBusinessIntelligenceDashboardsArgs";
import { BusinessIntelligenceDashboardsService } from "../businessIntelligenceDashboards.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => BusinessIntelligenceDashboards)
export class BusinessIntelligenceDashboardsResolverBase {
  constructor(
    protected readonly service: BusinessIntelligenceDashboardsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "BusinessIntelligenceDashboards",
    action: "read",
    possession: "any",
  })
  async _businessIntelligenceDashboardsItemsMeta(
    @graphql.Args() args: BusinessIntelligenceDashboardsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [BusinessIntelligenceDashboards])
  @nestAccessControl.UseRoles({
    resource: "BusinessIntelligenceDashboards",
    action: "read",
    possession: "any",
  })
  async businessIntelligenceDashboardsItems(
    @graphql.Args() args: BusinessIntelligenceDashboardsFindManyArgs
  ): Promise<BusinessIntelligenceDashboards[]> {
    return this.service.businessIntelligenceDashboardsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => BusinessIntelligenceDashboards, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "BusinessIntelligenceDashboards",
    action: "read",
    possession: "own",
  })
  async businessIntelligenceDashboards(
    @graphql.Args() args: BusinessIntelligenceDashboardsFindUniqueArgs
  ): Promise<BusinessIntelligenceDashboards | null> {
    const result = await this.service.businessIntelligenceDashboards(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => BusinessIntelligenceDashboards)
  @nestAccessControl.UseRoles({
    resource: "BusinessIntelligenceDashboards",
    action: "delete",
    possession: "any",
  })
  async deleteBusinessIntelligenceDashboards(
    @graphql.Args() args: DeleteBusinessIntelligenceDashboardsArgs
  ): Promise<BusinessIntelligenceDashboards | null> {
    try {
      return await this.service.deleteBusinessIntelligenceDashboards(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
