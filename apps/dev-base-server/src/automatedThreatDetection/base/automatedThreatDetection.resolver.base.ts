/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AutomatedThreatDetection } from "./AutomatedThreatDetection";
import { AutomatedThreatDetectionCountArgs } from "./AutomatedThreatDetectionCountArgs";
import { AutomatedThreatDetectionFindManyArgs } from "./AutomatedThreatDetectionFindManyArgs";
import { AutomatedThreatDetectionFindUniqueArgs } from "./AutomatedThreatDetectionFindUniqueArgs";
import { DeleteAutomatedThreatDetectionArgs } from "./DeleteAutomatedThreatDetectionArgs";
import { AutomatedThreatDetectionService } from "../automatedThreatDetection.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => AutomatedThreatDetection)
export class AutomatedThreatDetectionResolverBase {
  constructor(
    protected readonly service: AutomatedThreatDetectionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "AutomatedThreatDetection",
    action: "read",
    possession: "any",
  })
  async _automatedThreatDetectionsMeta(
    @graphql.Args() args: AutomatedThreatDetectionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [AutomatedThreatDetection])
  @nestAccessControl.UseRoles({
    resource: "AutomatedThreatDetection",
    action: "read",
    possession: "any",
  })
  async automatedThreatDetections(
    @graphql.Args() args: AutomatedThreatDetectionFindManyArgs
  ): Promise<AutomatedThreatDetection[]> {
    return this.service.automatedThreatDetections(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => AutomatedThreatDetection, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "AutomatedThreatDetection",
    action: "read",
    possession: "own",
  })
  async automatedThreatDetection(
    @graphql.Args() args: AutomatedThreatDetectionFindUniqueArgs
  ): Promise<AutomatedThreatDetection | null> {
    const result = await this.service.automatedThreatDetection(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => AutomatedThreatDetection)
  @nestAccessControl.UseRoles({
    resource: "AutomatedThreatDetection",
    action: "delete",
    possession: "any",
  })
  async deleteAutomatedThreatDetection(
    @graphql.Args() args: DeleteAutomatedThreatDetectionArgs
  ): Promise<AutomatedThreatDetection | null> {
    try {
      return await this.service.deleteAutomatedThreatDetection(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
